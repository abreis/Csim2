#include "Csim2.h"
using namespace std;

/*
 * TODO:
 * - store re-healing times in simulation
 * -- vector array w/ type(bestworst) srcVID dstVID rehealing
 * -- sum all times at the end and match against actual simEndTime-simStartTime(packet 1)
 *
 */

int main(int argc, char* argv[])
{

	return 1;
}
